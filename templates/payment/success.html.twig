{% extends 'base.html.twig' %}

{% block title %}Commande passée{% endblock %}

{% block body %}
    <header>
        <span><a href="{{ path('app_home') }}">Accueil</a> / <a href="{{ path('app_cart') }}">Mon Panier</a> / Paiement / Paiement Réussi</span>
        <h1>La commande a bien été enregistré</h1>

        <p style="margin-top: 40px">
            Merci pour votre commande, vous pouvez télécharger le récapitulatif de la commande directement dans la page de votre profil.<br>
            Vous aller être automatiquement redirigé vers votre profil dans 5 secondes.
        </p>

        <a style="margin-top: 15px" class="links" href="{{ path('app_profile') }}">Voir mon profil</a>
    </header>

    <script>
        // reset cart in cookies
        document.cookie = 'cart=; Max-Age=0; path=/; domain=' + location.hostname;

        setTimeout(function() {
            window.location.href = "{{ path('app_profile') }}";
        }, 5000);
    </script>
{% endblock %}
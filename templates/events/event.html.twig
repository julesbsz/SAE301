{% extends 'base.html.twig' %}

{% block title %}{{ event.name }}{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('events') }}
{% endblock %}

{% block body %}
    <header>
        <span><a href="/">Accueil</a> / <a href="{{ path('app_events') }}">Événements</a> / {{ event.name }}</span>
        <h1>{{ event.name }}</h1>
    </header>

    <section id="event">
        <img src="{{ asset("build/" ~ event.image) }}" alt="{{ event.name }}" class="img-fluid">

        <span>
            Lieux : {{ place.name }}<br>
            Adresse : {{ place.address }}<br>
            {{ place.capacity }} places disponibles<br>
            {{ event.genre }}<br>
            {{ event.date }}
        </span>

        <p>{{ event.description }}</p>


    </section>

    <section id="reserver"> 
            <a href="#" class="button">Commander une place</a>
        <span id="article">{{event.name}}</span>  <span id="prix"> {% if event.price == 0 %} Gratuit {% elseif event.price > 0.001   %} {{event.price}}€   {% endif %} 
    x <input type="number" value="1" min="1" max="99" id="qte">
    <button id="ajout">ajouter au panier</button></span>

    </section>

{% endblock %}
